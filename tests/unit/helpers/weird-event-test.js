import { weirdEvent } from 'get-out/helpers/weird-event';
import { module, test } from 'qunit';

module('Unit | Helper | weird event');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = weirdEvent([42]);
  assert.ok(result);
});
