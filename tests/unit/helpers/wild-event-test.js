import { wildEvent } from 'get-out/helpers/wild-event';
import { module, test } from 'qunit';

module('Unit | Helper | wild event');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = wildEvent([42]);
  assert.ok(result);
});
