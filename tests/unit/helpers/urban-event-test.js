import { urbanEvent } from 'get-out/helpers/urban-event';
import { module, test } from 'qunit';

module('Unit | Helper | urban event');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = urbanEvent([42]);
  assert.ok(result);
});
