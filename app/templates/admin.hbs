<div class="navbar">
  {{#link-to 'home-page'}}<h2 id="title">logo</h2>{{/link-to}}
  <div class="menu">
    {{#link-to 'profile'}}<div class="profileImage"></div>{{/link-to}}
    <h3 {{action 'showNav'}}>MENU</h3>
  </div>
  <div class="navigation hidden">
    <p>Browse All Events</p>
    {{#link-to 'index'}}<p>Sign Out</p>{{/link-to}}
  </div>
</div>

<h4>Update Events</h4>

{{new-friend-event admin-event=admin-event saveEvent='saveEvent'}}

<h3>Your Friend Events</h3>
{{#each model as |admin-event|}}
  {{admin-event-tile admin-event=admin-event}}
{{/each}}
